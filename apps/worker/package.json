{
  "name": "worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"worker lint not configured yet\"",
    "test": "echo \"worker tests not configured yet\""
  },
  "devDependencies": {
    "@types/node": "^22.20.2",
    "tsx": "latest",
    "typescript": "latest"
  }
}
